# App
APP_URL=https://nimbi-ofertas-programadas-pro.vercel.app
NODE_ENV=production

# Supabase Postgres (Prisma) — projeto dedicado Ofertas Pro
# NÃO use db.*.supabase.co aqui: esse host não resolve/IPv6 e o Prisma falha.
# Use Session pooler (porta 5432) do painel: Connect → Session pooler.
# User no pooler: postgres.<PROJECT_REF> | Senha URL-encoded: * → %2A
# Se falhar, confira a região no Connect do Supabase e ajuste o host aws-*-<region>.pooler...
DATABASE_URL=postgresql://postgres.euoiptfviigspfggcoii:ofertaspro2026%2A@aws-0-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require

# Supabase API (Storage / CDN) — mesmo projeto
SUPABASE_URL=https://euoiptfviigspfggcoii.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://euoiptfviigspfggcoii.supabase.co
# Cole as keys do projeto novo em Project Settings → API
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=ofertas-pro

# Nuvemshop App (Partner Portal) — Ofertas Pro
# Docs: https://tiendanube.github.io/api-documentation/authentication
NUVEMSHOP_CLIENT_ID=37315
NUVEMSHOP_CLIENT_SECRET=da6dd438a9172563f4500ca7eadfefc121cc3d4362ebbcd7
NEXT_PUBLIC_NUVEMSHOP_CLIENT_ID=37315

# Host OAuth: tiendanube (default LATAM) | nuvemshop (BR)
NUVEMSHOP_AUTH_HOST=nuvemshop

# Escopos sugeridos no Partner Portal:
#   write_products, write_content, write_scripts
#
# URLs no Partner Portal:
#   Redirect (callback):     {APP_URL}/api/auth/nuvemshop/callback
#   URL do app (Enhanced):   {APP_URL}/offers
#   Preferences URL:         {APP_URL}/offers
#   Store Redact webhook:         {APP_URL}/api/v1/webhooks/redact/store
#   Customer Redact webhook:      {APP_URL}/api/v1/webhooks/redact/customers
#   Customers Data Request:       {APP_URL}/api/v1/webhooks/redact/customers/data-request
#   Install authorize (opcional): {APP_URL}/api/auth/nuvemshop/authorize

NUVEMSHOP_WEBHOOK_SECRET=da6dd438a9172563f4500ca7eadfefc121cc3d4362ebbcd7

# Billing nativo (assinatura e trial = Nuvemshop)
NUVEMSHOP_SERVICE_ID=37315
NUVEMSHOP_BILLING_CONCEPT_CODE=app-cost
BILLING_PLAN_ID=
BILLING_PLAN_EXTERNAL_REF=ofertas-pro-mensal
BILLING_AMOUNT_VALUE=99.90
BILLING_AMOUNT_CURRENCY=BRL
# Validações de pagamento (gate de assinatura):
#   true  = ATIVA (bloqueia loja sem trial/active)
#   false = DESATIVA (libera acesso sem checar pagamento)
# Alias: PAYMENT_VALIDATION=true|false
# Sem a var: em production fica ATIVA; em development fica DESATIVADA.
BILLING_ENFORCE=false

# Cron externo (cron-job.org) — Authorization: Bearer …
# URL: {APP_URL}/api/cron/offers | schedule: */5 * * * *
CRON_SECRET=090be21b5a15396ba3f34849601015596138135b72f3bae8

# Criptografia do access_token no banco
TOKEN_ENCRYPTION_KEY=5a8e2a66a200793d802adb0404d8e518f8551843bf0a0c2cf41ae0f5480520cf

# Dev local: store_id já instalada no banco (pula JWT do Nexo)
# DEV_STORE_ID=
